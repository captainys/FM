SCSI_DATABUF			DB		256 dup (0)


						ALIGN	512
BOOTMENU_SELECTION		DB		BOOTMENU_ITEMNUM_SCSI_CD		;  BOOT MENU Default Option
						DB		1023 dup(0)

PARTITION_TABLE			EQU		BOOTMENU_SELECTION+1024

; Used in DRVMENU and BOOTMENU
MENU_EXPAND_BUF			EQU		PARTITION_TABLE+2048
