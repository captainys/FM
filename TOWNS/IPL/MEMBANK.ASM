VRAMMODE:				PUSH	DX
						PUSH	AX
						MOV		AL,0
						JMP		SHORT SET_VRAM_OR_RAM_MODE

RAMMODE:				PUSH	DX
						PUSH	AX
						MOV		AL,80H

SET_VRAM_OR_RAM_MODE:
						MOV		DX,IO_FMR_VRAM_OR_MAINRAM
						OUT		DX,AL
						POP		AX
						POP		DX
						RET
